# The dashboard is its own git repository (deployed separately via Amplify
# Hosting) — never track it from this repo.
dashboard/

# Claude Code local config
.claude/

# Credentials / signing secrets — never commit these anywhere in the tree
*.p8
*.p12
*.key
*.keystore
*.jks
*.mobileprovision
.env
.env.*
!.env.example
